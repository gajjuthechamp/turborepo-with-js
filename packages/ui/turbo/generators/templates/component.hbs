import * as React from "react";

interface Props {
  children?: React.ReactNode;
}

export const {{ pascalCase name }} = ({ children }) => {
  return (
    <div>
      <h1>{{ name }}</h1>
      {children}
    </div>
  );
};
